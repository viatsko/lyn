<h2>Edit</h2>

<%= render "form.html", changeset: @changeset,
                        languages: @languages,
                        model: @model,
                        action: admin_admin_path(@conn, :update, @resource, @entry) %>

<%= link "Back", to: admin_admin_path(@conn, :index, @resource) %>
